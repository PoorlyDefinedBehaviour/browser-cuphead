export class Animations {
  public static cuphead: any = {
    idle: Symbol(),
    run: { normal: Symbol(), gun: Symbol() },
    jump: Symbol(),
    death: Symbol(),
    shoot: Symbol()
  };

  public static cagney: any = {
    intro: Symbol(),
    idle: Symbol(),
    create_object: Symbol(),
    death: Symbol(),
    firing_seeds: Symbol()
  };

  public static skills: any = {
    missile: null
  };
}
